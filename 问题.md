第一个问题
"AssertionError: OSC_POSE controller is specified, but not imported or loaded"
   1. 安装robosuite_models："pip install robosuite_models"

第二个问题
```txt
[robosuite WARNING] No private macro file found! (macros.py:53)
[robosuite WARNING] It is recommended to use a private macro file (macros.py:54)

解决：
python /home/benson/anaconda3/envs/cpgen/lib/python3.10/site-packages/robosuite/scripts/setup_macros.py
```

第三个问题
```txt
ROBOMIMIC WARNING(
    No private macro file found!
    It is recommended to use a private macro file
)

解决：
python /home/benson/anaconda3/envs/cpgen/lib/python3.10/site-packages/robomimic/scripts/setup_macros.py
```

第四个问题
```txt
  File "/home/user/robosuite/robosuite/controllers/composite/__init__.py", line 8, in composite_controller_factory
    assert type in REGISTERED_COMPOSITE_CONTROLLERS_DICT, f"{type} controller is specified, but not imported or loaded"
AssertionError: OSC_POSE controller is specified, but not imported or loaded

解决：
https://github.com/ARISE-Initiative/robosuite/issues/581
from robosuite.controllers import load_composite_controller_config

env_meta["env_kwargs"]["controller_configs"] = load_composite_controller_config(controller="BASIC")
```

